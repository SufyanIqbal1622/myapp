json.extract! mobile, :id, :name, :company, :model, :file?, :and, :present?, :checks, :if, :the, :_file_name, :field, :is, :populated, :created_at, :updated_at
json.url mobile_url(mobile, format: :json)
